#include <algorithm>
#include <climits>
#include <cmath>
#include <cstdio>
#include <iostream>
#include <vector>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    // freopen("angry.in", "r", stdin);
    // freopen("angry.out", "w", stdout);

    int k, n;
    cin >> k >> n;

    int matrix[n][n];

    for (int i = 0; i < k; i++) {
        if ()
    }

    return 0;
}
